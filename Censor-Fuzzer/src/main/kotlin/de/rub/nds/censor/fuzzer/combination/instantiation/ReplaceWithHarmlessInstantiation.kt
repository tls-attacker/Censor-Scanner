package de.rub.nds.censor.fuzzer.combination.instantiation

/**
 * Whether to replace the hostname with a harmless hostname.
 */
enum class ReplaceWithHarmlessInstantiation : Instantiation {
    DEFAULT,
    HARMLESS
}